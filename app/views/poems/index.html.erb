<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Wallace Stevens Society</title>
  <% @page_title="Wallace Stevens Society" %>
  <!--<link rel="stylesheet" href="css/normalize.css">
  <link rel="stylesheet" href="css/style.css"> -->
  <%= stylesheet_link_tag('normalize', :media =>'all') %>
  <%= stylesheet_link_tag('index_poem', :media =>'all') %>
  <%= stylesheet_link_tag('owl.carousel.css', :media => 'all') %>
  <%= stylesheet_link_tag('owl.theme.css', :media => 'all') %>
  <link href='https://fonts.googleapis.com/css?family=PT+Serif:400,400italic,700,700italic' rel='stylesheet' type='text/css'>
</head>
<body>
  <header>
    <div class="container">
      <div class="logo"><%= link_to image_tag('logo.png'), root_path %></div>
        <nav>
          <ul>
            <li>
              <a href="#">About</a>
              <ul class="fallback">
                <li><a href="about">About Wallace Stevens</a></li>
                <li><a href="#">The Wallace Stevens Society</a></li>
                <li><a href="#">Upcoming Events </a></li>
              </ul>
            </li>
            <li>
              <a href="#">Journal</a>
              <ul class="fallback">
                <li><a href="#">Call for Papers</a></li>
                <li><a href="#">Listing of Articles</a></li>
              </ul>
            </li>
            <li><a href="#">Resources</a></li>
            <li><a href="#">Online Concordance</a></li>
          </ul>
        </nav>

    </div>
  </header>
<p id="notice"><%= notice %></p>
<div class="maintext">
<h3>WALLACE STEVENS</h3><h1>ONLINE CONCORDANCE</h1>

  <%= form_tag(poems_path, :method => "get", class: "navbar-form", id: "search-form") do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search Wallace Stevens Works" %>
      <!-- In order to have the "search" icon int the button, we need to use plain HTML instead 
           of using a Rails form helper -->
      <button class="btn" type="submit">Search</button>
       <% end %>
    </div>

<table>
  <thead>
    <tr class="form-row">
      <th class="ptitle">Title</th>
      <th class="pyear">Year Published</th>
      <th class="pbook">Publication</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @poems.each do |poem| %>
      <tr class="form-row">
        <td class="ptitle"><%= link_to poem.title, poem %></td>
        <td class="pyear"><%= poem.year %></td>
        <td class="pbook"><%= poem.book %></td>
        <td><%= link_to 'Show', poem %></td>
        <td><%= link_to 'Edit', edit_poem_path(poem) %></td>
        <td><%= link_to 'Destroy', poem, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Poem', new_poem_path %> 
</div>
<%= javascript_include_tag("public")%>
<%= javascript_include_tag("index")%>
<%= javascript_include_tag("owl.carousel.js") %>
</body>
</html>
<body>

